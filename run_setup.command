#!/bin/bash
# ============================================================
# 发票夹子 · macOS 一键配置向导
# 使用方式：双击运行，或在终端执行 ./run_setup.command
# ============================================================
cd "$(dirname "$0")"
python3 setup_config.py
